#AddVerseForm
1. Add a cursor to the verse initials field to make editing that string possible.
2. This may be closely related to the previous item. If the verse initials input exceeds the space, the content needs to scroll horizontally so that the most recent inputs are always visible.
3. Have a scroll anchor on the verse initials field so when the user clicks in that field and the onscreen keyboard appears, the viewport is positioned so that the verse initials field is just above the top edge of the onscreen keyboard. Currently it scrolls way down and the user has to scroll back up to see the chinese text.
4. The physical keyboard doesn’t work for inputting numbers on the add verse form.
#LearningFlow
5. Add a retry button to learning flow modals after the basic, and intermediate stages. 
6. Clear the onscreen keyboard feedback in learningFlow when a difficulty button is selected.
#Toast Notifications
7. Display achievement and streak notifications with a higher z-index than the modal overlay
#Speed challenge for a collection
8. There needs to be padding at the bottom of the screen so that the auto scroll can work. Currently the user can’t scroll down when there are more than 3-4 verses loaded.
#Reverse by verse
9. The navigation buttons get stuck below the onscreen keyboard. There needs to be more padding below the verse-display to keep this onscreen. Should be 200px
10. In this same practice activity the auto scroll is not working. If the next hidden character is below the keyboard it doesn’t auto scroll to bring it in view. But if the user scrolls so that it is visible, the auto scroll incrementally scrolls up after each input forcing the text in focus off screen below the onscreen keyboard. This is the opposite of what we want.
11. The completion modal shows that it is calculating the accuracy of the entire activity (for example 324/337 when the total characters in a collection is only say 200). Instead, it should just calculate the accuracy of the final phase of the activity.
#Review a collection
12. When the user selects to review a collection and review the verses individually, there should be an option to review the verses in collection order. So the options list for this use case should be: Collection Order, Biblical Order, Reverse Biblical Order, By Due Date, Random Order.
#Practice a Verse
13. For selecting a verse to practice group verses by book and chapter with an expandable/collapsible structure (like the My Verses section on the add verse form) to reduce scrolling/
Stats Progress Chart
14. Add buttons below the progress chart that switches between All time, 30 days, and 7 days. 
15. On the all time view, since the app purges older data points only keeping weekly data points, the chart should take that into account so that past periods of 30 days take up the same amount of space as the most recent 30 days even though there are likely to be more data points for the past 30 days. On the x-axis label the date of the first data point, and mark the last data point as Today.
15. Add numbers of verses to the y axis 
16. The timeline progress chart should have twice the height it does now.
#Heatmaps
17. Include a key that shows the gradient of color from 99 to 0 that our heatmap system uses and put labels at 80, 60, 40, and 20.